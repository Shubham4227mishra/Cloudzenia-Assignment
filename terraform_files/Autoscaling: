resource "aws_appautoscaling_target" "ecs" {
  max_capacity       = 2
  min_capacity       = 1
  resource_id        = "service/${aws_ecs_cluster.main.name}/wordpress"
  scalable_dimension = "ecs:service:DesiredCount"
  service_namespace  = "ecs"
}

